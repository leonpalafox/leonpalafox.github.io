---
interface Props {
  title: string;
  id?: string;
}
const { title, id } = Astro.props;
---

<h2 id={id} class="text-2xl font-semibold tracking-tight">
  {title}
  <span aria-hidden="true" class="mt-2.5 block h-[3px] w-10 bg-signal"></span>
</h2>
